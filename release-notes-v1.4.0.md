# Xiaomi13Font v1.4.0

本版本修复全系统 OverlayFS 晚挂载后，小米笔记编辑页和后来启动的应用恢复系统默认字体的问题。

- 只接受本次开机更新的 OverlayFS 成功日志，不再误读旧记录。
- 验证 `/system` 和 `/product` 的 OverlayFS 实际挂载。
- 连续三次确认稳定并额外等待 8 秒。
- 字体绑定后执行 SHA-256 校验。
- 失败自动重试，最多三轮。
- 保留原始 1.0 六按钮界面和全部功能。
- OverlayFS 延迟兼容模式仍可勾选或取消。
- 不包含任何第三方字体。
